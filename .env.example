# ─────────────────────────────────────────────────────────────────────────────
# SoHo onboarding SPA — Supabase (client-side, build-time inlined by Vite)
# The anon key is publishable-class by design; access is enforced by RLS.
# NEVER put a service_role / secret key here or anywhere in the repo.
# ─────────────────────────────────────────────────────────────────────────────
VITE_SUPABASE_URL=https://ugkbcnnaewdfdyibpvdn.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InVna2Jjbm5hZXdkZmR5aWJwdmRuIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODY3Nzk1ODcsImV4cCI6MjEwMjM1NTU4N30.ESEGhkDJscunbSy85kStEOPRxUQtRALPXZpgRIIUXEw

# ─────────────────────────────────────────────────────────────────────────────
# PayOS payment server (server-side only — set on Railway, NEVER bundled to client)
# These names must match what the deployed Railway service already uses so a
# redeploy over the same service needs zero env changes. Values below are blank
# placeholders; the real secrets live only in Railway's environment.
# ─────────────────────────────────────────────────────────────────────────────
PAYOS_CLIENT_ID=
PAYOS_API_KEY=
PAYOS_CHECKSUM_KEY=
PAYOS_PARTNER_CODE=

# A long random token used only for trusted server-to-server API calls.
# Never put this token in browser or mobile application code.
PAYOS_INTERNAL_API_TOKEN=

# Public pages shown after the customer leaves payOS Checkout.
PAYOS_RETURN_URL=https://your-domain.example/payment/success.html
PAYOS_CANCEL_URL=https://your-domain.example/payment/cancel.html

# Public webhook endpoint. Register it with the script after deployment.
# PayOS has this path registered — keep it identical: /api/payos/webhook
PAYOS_WEBHOOK_URL=https://your-domain.example/api/payos/webhook

# The existing order service that will atomically mark an order as paid.
# Real payment webhooks return HTTP 503 until this URL is configured.
PAYOS_WEBHOOK_FORWARD_URL=
PAYOS_WEBHOOK_FORWARD_TOKEN=

# Optional SDK logging: off, error, warn, info, or debug.
PAYOS_LOG=warn
